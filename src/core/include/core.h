#ifndef CORE_HPP
#define CORE_HPP

#include "module.h"

class Core {
    private:

    public:
        Core() {};
        ~Core() {};

        void load() {};
        void unload() {};
};

#endif